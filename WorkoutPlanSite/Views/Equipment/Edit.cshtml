@model EquipmentInputModel
@{
    ViewData["Title"] = "Editing equipment";
    ViewBag.Action = "Edit";
}

<h2 class="text-center mb-2">@ViewData["Title"]</h2>
<hr />
<div class="container">
    <partial name="_EquipmentFormPartial" model="@Model" view-data="ViewData" />
</div>